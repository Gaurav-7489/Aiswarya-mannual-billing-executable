import { NextResponse } from 'next/server'
import { requireApiAuth } from '@/lib/auth/api'
import { selectOne, select } from '@/lib/db/client'
export const runtime='nodejs'
export async function GET(_request:Request,context:{params:Promise<{id:string}>}){const guard=await requireApiAuth();if(guard.response)return guard.response;const{id}=await context.params,raw=await selectOne<any>('invoices',`select=*,customers(name,code,phone,email,gstin,billing_address,shipping_address,city,state,pincode)&id=eq.${encodeURIComponent(id)}`);if(!raw)return NextResponse.json({ok:false,error:'Invoice not found.'},{status:404});const c=raw.customers||{},invoice={...raw,customer_name:c.name,customer_code:c.code,customer_phone:c.phone,customer_email:c.email,customer_gstin:c.gstin,billing_address:c.billing_address,shipping_address:c.shipping_address,customer_city:c.city,customer_state:c.state,customer_pincode:c.pincode};delete invoice.customers;const items=await select<any>('invoice_items',`select=*&invoice_id=eq.${encodeURIComponent(id)}&order=created_at.asc`);return NextResponse.json({ok:true,invoice,items})}
