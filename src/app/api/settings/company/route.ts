import { NextResponse } from 'next/server'
import { requireApiAuth } from '@/lib/auth/api'
import { getCompanySettings, saveCompanySettings, type CompanySettings } from '@/lib/settings/company'
export const runtime='nodejs'
export async function GET(){const guard=await requireApiAuth();if(guard.response)return guard.response;return NextResponse.json({settings:getCompanySettings()})}
export async function PUT(request:Request){const guard=await requireApiAuth('OWNER');if(guard.response)return guard.response;try{const body=await request.json() as Partial<CompanySettings>;const allowed:Array<keyof CompanySettings>=['name','tagline','address','businessDescription','productRange','workingHours','gstin','fssai','phone','email','website','logoUrl','bankAccountName','bankName','bankAccountNumber','bankIfsc','bankBranch','terms','footerMessage','footerTagline','authorizedSignatory','defaultGstMode'];const input:Partial<CompanySettings>={};for(const key of allowed)if(body[key]!==undefined)input[key]=String(body[key]) as never;if(input.defaultGstMode!==undefined&&input.defaultGstMode!=='INTRA'&&input.defaultGstMode!=='INTER')return NextResponse.json({error:'GST mode must be INTRA or INTER.'},{status:400});return NextResponse.json({settings:saveCompanySettings(input)})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Unable to save company settings.'},{status:400})}}
