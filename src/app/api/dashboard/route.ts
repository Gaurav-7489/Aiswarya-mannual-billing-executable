import { NextResponse } from 'next/server'
import { requireApiAuth } from '@/lib/auth/api'
import { getDatabase } from '@/lib/db/client'
export const runtime='nodejs'
export async function GET(){const guard=await requireApiAuth();if(guard.response)return guard.response;const db=getDatabase();const today=new Date().toISOString().slice(0,10);const summary=db.prepare(`SELECT COALESCE(SUM(CASE WHEN status='FINALIZED' THEN grand_total_minor ELSE 0 END),0) AS todaySalesMinor,COUNT(CASE WHEN status='FINALIZED' THEN 1 END) AS todayInvoiceCount,COUNT(CASE WHEN status='DRAFT' THEN 1 END) AS draftCount,COUNT(CASE WHEN status='CANCELLED' THEN 1 END) AS cancelledCount FROM invoices WHERE invoice_date=?`).get(today) as Record<string,number>;const counts=db.prepare(`SELECT (SELECT COUNT(*) FROM customers WHERE is_active=1) AS customerCount,(SELECT COUNT(*) FROM products WHERE is_active=1) AS productCount,(SELECT COUNT(*) FROM notifications WHERE status='FAILED') AS failedNotificationCount`).get() as Record<string,number>;const recentInvoices=db.prepare(`SELECT i.id,i.invoice_number AS invoiceNumber,i.invoice_date AS invoiceDate,i.status,i.grand_total_minor AS grandTotalMinor,c.name AS customerName FROM invoices i JOIN customers c ON c.id=i.customer_id ORDER BY i.invoice_date DESC,i.created_at DESC LIMIT 8`).all();const topCustomers=db.prepare(`SELECT c.id,c.name,c.code,COUNT(i.id) AS invoiceCount,COALESCE(SUM(CASE WHEN i.status='FINALIZED' THEN i.grand_total_minor ELSE 0 END),0) AS totalSalesMinor FROM customers c LEFT JOIN invoices i ON i.customer_id=c.id WHERE c.is_active=1 GROUP BY c.id ORDER BY totalSalesMinor DESC,invoiceCount DESC LIMIT 5`).all();return NextResponse.json({today,summary:{todaySalesMinor:Number(summary.todaySalesMinor??0),todayInvoiceCount:Number(summary.todayInvoiceCount??0),draftCount:Number(summary.draftCount??0),cancelledCount:Number(summary.cancelledCount??0)},counts:{customerCount:Number(counts.customerCount??0),productCount:Number(counts.productCount??0),failedNotificationCount:Number(counts.failedNotificationCount??0)},recentInvoices,topCustomers})}
